ValidateOtp:
  Prereg_ValidateOtp_All_Valid_Smoke_email_sid:
    endPoint: /preregistration/v1/login/validateOtp
    uniqueIdentifier: TC_prereg_ValidateOtp_01
    description: Validate that entering a valid OTP for email is successfully processed when all other mandatory fields are valid
    role: batch
    checkErrorsOnlyInResponse: true
    restMethod: post
    inputTemplate: preReg/ValidateOtp/validateOtp
    outputTemplate: preReg/ValidateOtp/validateOtpResult
    input: '{
      "version": "1.0",
      "requesttime": "$TIMESTAMP$",
      "userId": "$EMAILVALUE$",
      "otp": "DYNAMICOTP",
      "id": "mosip.pre-registration.login.useridotp"
    }'
    output: '{}'

  Prereg_ValidateOtp_All_Valid_Smoke_Phone_sid:
    endPoint: /preregistration/v1/login/validateOtp
    uniqueIdentifier: TC_prereg_ValidateOtp_02
    description: Validate that entering a valid OTP for phone is successfully processed when all other mandatory fields are valid
    role: batch
    restMethod: post
    inputTemplate: preReg/ValidateOtp/validateOtp
    outputTemplate: preReg/error
    input: '{
      "version": "1.0",
      "requesttime": "$TIMESTAMP$",
      "userId": "$PHONENUMBER$",
      "otp": "DYNAMICOTP",
      "id": "mosip.pre-registration.login.useridotp"
    }'
    output: '{
      "errors": [
        {
          "errorCode": "KER-OTV-005"
        }
      ]
    }'

  Prereg_ValidateOtp_Invalid_Phone_Neg_INVALIDOTP:
    endPoint: /preregistration/v1/login/validateOtp
    uniqueIdentifier: TC_prereg_ValidateOtp_03
    description: Validate that entering an invalid OTP for phone is rejected and an appropriate error message is displayed
    role: batch
    checkErrorsOnlyInResponse: true
    restMethod: post
    inputTemplate: preReg/ValidateOtp/validateOtp
    outputTemplate: preReg/error
    input: '{
      "version": "1.0",
      "requesttime": "$TIMESTAMP$",
      "userId": "909",
      "otp": "DYNAMICOTP",
      "id": "mosip.pre-registration.login.useridotp"
    }'
    output: '{
      "errors": [
        {
          "errorCode": "KER-OTV-005"
        }
      ]
    }'

  Prereg_ValidateOtp_SpacialCharacters_Phone_Neg_INVALIDOTP:
    endPoint: /preregistration/v1/login/validateOtp
    uniqueIdentifier: TC_prereg_ValidateOtp_04
    description: Validate that entering an OTP with special characters for phone is rejected and an appropriate error message is displayed
    role: batch
    checkErrorsOnlyInResponse: true
    restMethod: post
    inputTemplate: preReg/ValidateOtp/validateOtp
    outputTemplate: preReg/error
    input: '{
      "version": "1.0",
      "requesttime": "$TIMESTAMP$",
      "userId": "$!(",
      "otp": "DYNAMICOTP",
      "id": "mosip.pre-registration.login.useridotp"
    }'
    output: '{
      "errors": [
        {
          "errorCode": "KER-OTV-005"
        }
      ]
    }'

  Prereg_ValidateOtp_SpacialCharacters_Email_Neg_INVALIDOTP:
    endPoint: /preregistration/v1/login/validateOtp
    uniqueIdentifier: TC_prereg_ValidateOtp_05
    description: Validate that entering an OTP with special characters for email is rejected and an appropriate error message is displayed
    role: batch
    checkErrorsOnlyInResponse: true
    restMethod: post
    inputTemplate: preReg/ValidateOtp/validateOtp
    outputTemplate: preReg/error
    input: '{
      "version": "1.0",
      "requesttime": "$TIMESTAMP$",
      "userId": "john.doe@mail$!inator.com",
      "otp": "DYNAMICOTP",
      "id": "mosip.pre-registration.login.useridotp"
    }'
    output: '{
      "errors": [
        {
          "errorCode": "KER-OTV-005"
        }
      ]
    }'

  Prereg_ValidateOtp_Invalid_Email_Neg_INVALIDOTP:
    endPoint: /preregistration/v1/login/validateOtp
    uniqueIdentifier: TC_prereg_ValidateOtp_06
    description: Validate that entering an invalid OTP for email is rejected and an appropriate error message is displayed
    role: batch
    checkErrorsOnlyInResponse: true
    restMethod: post
    inputTemplate: preReg/ValidateOtp/validateOtp
    outputTemplate: preReg/error
    input: '{
      "version": "1.0",
      "requesttime": "$TIMESTAMP$",
      "userId": "joh23",
      "otp": "DYNAMICOTP",
      "id": "mosip.pre-registration.login.useridotp"
    }'
    output: '{
      "errors": [
        {
          "errorCode": "KER-OTV-005"
        }
      ]
    }'